#include "core.hpp"
int main(){
    {
        core pks;
        pks.game();
    }

    // hc(sizeof(Corn::User));
    // hc(sizeof(Corn::TrainInfo));
    // hc(sizeof(Corn::Order));
    // hc(sizeof(Corn::Seat));
    // hc(sizeof(Corn::StationName));
    // std::cerr<<"readwrite"<<sumCheck<<std::endl;
    // {
    //     pksBPT QQ("daya.in");
    //     QQ.clean();
    //     for (int i=1;i<=10;i++)
    //         printf("%d\n",QQ.Insert(std::pair<long long ,long long> (666*i,777*i),i));
    // }
    // {
    //     pksBPT QQ("daya.in");
    //     for (int i=1;i<=10;i++)
    //             printf("%d\n",QQ.Find(std::pair<long long ,long long> (666*i,777*i)));
    //     int A[30000];
    //     int x=QQ.Findinterval(std::pair<long long ,long long> (-1,0),std::pair<long long ,long long> (6666,7777),A);
    //     for (int i=0;i<x;i++)
    //         printf("%d\n",A[i]);
    // }   
}
